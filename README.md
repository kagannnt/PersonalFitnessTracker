#  Проект: Personal Fitness Tracker (Assignment 2)

###  Краткое описание проекта
**Personal Fitness Tracker** — это объектно-ориентированное приложение на Java, созданное для управления процессом тренировок.
Приложение позволяет пользователю:
1. Пройти регистрацию, указав свои антропометрические данные (рост и вес).
2. Выбрать направление тренировок (Кардио или Силовые) в зависимости от личных целей.
3. Получить автоматически сформированный план тренировок, отсортированный по времени.
4. Рассчитать персональный расход калорий после выполнения упражнения, исходя из веса пользователя и коэффициента сложности.

---

### Перечень выполненных требований ассайнмента

В ходе выполнения работы были реализованы следующие требования:

* **Subject Area (Предметная область):** Выбрана и проработана тема персонального фитнес-трекера.
* **Data Abstraction (Абстракция данных):** Создан абстрактный класс `Workout`, который инкапсулирует общие свойства всех видов активности.
* **Encapsulation (Инкапсуляция):** Все поля классов объявлены как `private`, доступ к ним реализован через конструкторы и публичные методы (геттеры).
* **Inheritance (Наследование):** Классы `Cardio` и `Strength` наследуют базовую логику от `Workout`, расширяя её специфичными расчетами.
* **Polymorphism (Полиморфизм):** Реализован через переопределение абстрактного метода `calculateCalories()`. Программа вызывает нужный метод в зависимости от типа объекта.
* **Data Pool (Пул данных):** Организовано хранилище всех доступных тренировок в виде `List<Workout>`.
* **Filtering, Searching, Sorting (Фильтрация, Поиск и Сортировка):**
    * **Фильтрация:** Выбор тренировок только определенного типа (Cardio/Strength).
    * **Поиск:** Нахождение конкретной тренировки по выбору пользователя.
    * **Сортировка:** Список планов выводится в порядке возрастания длительности (от коротких к длинным).
* **Override Methods (Переопределение стандартных методов):** В проекте корректно переопределены методы:
    * `toString()` — для красивого вывода информации в консоль.
    * `equals()` — для сравнения объектов тренировок.
    * `hashCode()` — для обеспечения уникальности объектов в коллекциях.
* **Main Class:** Создан класс `Main` (или `FitnessApp`), который объединяет всю логику и выводит результат работы программы.

---

### 👨‍💻 Автор
* **Студент:** Tasbolat Kagan
* **Группа:** SE-2502